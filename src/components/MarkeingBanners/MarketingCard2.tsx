import React from "react";
import { Link } from "react-router-dom";

interface CardProps {
  children: React.ReactNode;
  buttonLabel: string;
  onClick?: () => void;
  variant?: "blue" | "orange";
}

export default function MarketingCard2() {
  return <div></div>;
}
